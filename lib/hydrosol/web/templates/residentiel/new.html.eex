<h2>New Residentiel</h2>

<%= render "form.html", changeset: @changeset,
                        action: residentiel_path(@conn, :create) %>

<span><%= link "Back", to: residentiel_path(@conn, :index) %></span>
